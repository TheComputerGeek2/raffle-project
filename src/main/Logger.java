package main;

public class Logger {
	
	private boolean isActive;
	
public Logger() {
	//TODO create a log in the H drive
	//TODO create a config file and a means to enable or disable the logging
	/*
	 * Information to log:
	 * Minimum value, maximum value, ending value, seed, date
	 * 
	 */
	
	/*
	 * Logging options:
	 * where to keep the log
	 * if a log should be kept
	 */
}
}
